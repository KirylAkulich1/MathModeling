import time


def get_middle_interval(number:int,interal_length:int)->int:
    assert(interal_length%2 is 0)
    number_str:str = str(number)


def middle_square_generator(seed:int)->int:
    interval_length:int = len(str(seed))
    previous_value= seed
    while True:
        next_value = 

if __name__=="__main__":
    seed_str = str(time.time()).replace('.','')[-8:]
    seed= int(seed_str)
    print(seed)



